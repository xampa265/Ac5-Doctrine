
{% extends "base.html.twig" %}

        {% block enlaces %}
        <!-- navbar -->
        <nav class="nav nav-masthead">
          <a class="nav-link active" href="/inicio">Inicio</a>
          <a class="nav-link" href="/subir">Subir Datos</a>
          <a class="nav-link" href="/topagents">Estadisticas</a>
          <a class="nav-link" href="/Eventos">Eventos</a>
          <a class="nav-link" href="/contacto">Contacto</a>
          <a class="nav-link" href="/salir" >Cerrar sesion</a>
        </nav>
        
        <!-- navbar -->
        {% endblock %}

        
        {% block layout %}
        
        <main class="inner cover">
            <h1 class="cover-heading">Bienvenido {{session.username}}!</h1>
            <table class="tabla">
            <thead>

            <!-- Error Message -->
                <h4>{{ mensaje }}</h4>
             
              {% for row in results|slice(0,1) %}
              <tr> <th>Nivel<th>  <td> {{row.getLevel()}}</td>  </tr>
              <tr> <th>Livetime_ap<th>  <td> {{row. getLifetimeAp()}}</td>  </tr>
              <tr> <th>Current_ap<th>  <td> {{row.getCurrentAp()}}</td>  </tr> 
              {% endfor %}          
            </table>

           
            
          </main>
          
        {% endblock %}